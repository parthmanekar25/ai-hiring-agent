# ============================================
# AI HIRING AGENT - CLEANED REQUIREMENTS
# ============================================
# This is a cleaned and conventional requirements file.
# Notes:
# - The original file `requirement.txt` (singular) is preserved.
# - I removed two non-standard package names that commonly cause install failures:
#     - `langchain-openai` (no such PyPI package)
#     - `langchain-community` (community integrations should be installed explicitly)
# - Keep `langchain` and `openai` (the official packages) and install any extra community integrations separately.
# - If you prefer pinned versions for `langchain` or `openai`, we can pin them after you confirm compatibility.
# Installation (from project root):
# python -m venv venv
# source venv/bin/activate
# pip install -r requirements.txt
# ============================================

# ============================================
# BACKEND DEPENDENCIES
# ============================================

# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# File Processing
python-multipart==0.0.6
PyPDF2==3.0.1
aiofiles==23.2.1

# AI/LLM Libraries
langchain
langchain-groq
# If you need community integrations, install them individually, e.g.:
# pip install langchainhub-client  # example placeholder depending on integration

langgraph==0.0.20

# Environment & Configuration
python-dotenv==1.0.0

# HTTP Client (for internal requests)
httpx==0.25.2
requests==2.31.0

# Data Processing
pandas==2.1.3
numpy==1.26.2

# ============================================
# FRONTEND DEPENDENCIES
# ============================================

# Streamlit Framework
streamlit==1.29.0

# Visualization
plotly==5.18.0
altair==5.1.2

# Data Display
pandas==2.1.3

# ============================================
# DEVELOPMENT & TESTING DEPENDENCIES
# ============================================

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Code Quality
black==23.12.0
flake8==6.1.0
isort==5.13.2
mypy==1.7.1

# Type Stubs
types-requests==2.31.0.10

# ============================================
# OPTIONAL DEPENDENCIES
# ============================================

# Better Development Server (alternative to uvicorn)
gunicorn==21.2.0  # Unix only, for production

# Database (if you want to add persistence later)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL
pymongo==4.6.0  # MongoDB

# Caching (if you want to add Redis later)
redis==5.0.1
hiredis==2.3.2

# Monitoring & Logging
loguru==0.7.2
sentry-sdk==1.39.1

# ============================================
# SYSTEM DEPENDENCIES NOTES
# ============================================
# Some packages may require system-level dependencies (see original file for notes).
# ============================================
